---

# 数值词条流小说阅读增强插件 PRD（简化版）

> 平台：Legado（阅读）  
> 产品形态：Legado 插件 / 主题包 / 替换规则 + JS 脚本  
> 版本：MVP  
> 更新时间：2026-08-19

---

## 1. 背景与问题定义

### 1.1 背景
数值词条流小说包含大量游戏化内容：属性面板、装备词条、技能面板、系统提示、任务面板、奖励选择等。传统阅读器将这些内容以纯文本展示，缺乏辨识度和沉浸感。

### 1.2 问题
- 面板、装备、技能等结构化信息与普通正文混在一起，难以区分。
- 稀有度、数值变化、系统提示等元素无法直观呈现。
- 没有针对该小说类型的阅读增强方案。

### 1.3 解决方案
开发 Legado 插件，自动识别并增强展示数值词条流小说的特殊内容：
- **面板类内容**：正文中只保留可点击名称入口，点击后弹出统一风格的通用面板。
- **系统提示/任务/奖励/确认等内容**：正文内直接框起来，保持原文不删减。
- **数值增减**：高亮显示。
- **稀有度颜色**：名称按稀有度着色，支持自定义。

---

## 2. 目标用户与核心场景

### 2.1 目标用户
- 数值词条流小说读者，Legado 用户。
- 喜欢沉浸式阅读、对游戏化 UI 有偏好的网文读者。

### 2.2 核心场景
1. 读者打开数值词条流小说。
2. 插件自动识别当前页中的面板类内容和系统界面类内容。
3. 面板类内容的名称显示为带下划线的可点击入口，颜色按稀有度显示；无稀有度则使用正文样式+下划线。
4. 点击名称后弹出通用面板，字段名和字段值样式区分，标题栏应用稀有度颜色。
5. 系统提示以单行小框展示，任务/奖励/确认等以大框展示，内容原样保留。
6. 数值增减在正文中高亮。
7. 用户可自定义稀有度颜色、切换弹窗 UI 预设、导入导出配置。

---

## 3. 功能需求

### 3.1 优先级总览

| 优先级 | 功能                                                         |
| ------ | ------------------------------------------------------------ |
| P0     | 自动识别面板类内容；通用弹窗展示；稀有度颜色；系统提示单行框；系统任务/奖励大框；数值增减高亮 |
| P1     | 弹窗 UI 预设切换；稀有度颜色自定义；导入导出配置；规则更新入口；数值动效开关 |
| P2     | 预设图标库；AI 图片生成；手动标记语法；面板历史记录          |

### 3.2 P0 功能详情

#### 3.2.1 自动识别与渲染
- 实时扫描当前页文本，识别面板类内容和系统界面类内容。
- 识别只影响显示层，不修改原文。
- 识别错误时自动降级为普通文本。

#### 3.2.2 面板类内容（弹窗）
**适用对象**：属性面板、装备面板、技能面板等结构化键值对内容。

**识别触发**：
- 以 `名称：`、`生命值：`、`法力值：` 等属性字段连续出现。
- 以 `【名称（稀有度）】` 或 `【名称】` 开头，后跟 `字段名：字段值` 行。
- 以 `【技能名：Lv.X（类型）】` 开头的独立技能面板。

**展示方式**：
- 正文中只保留可点击名称入口。
- 名称入口样式：
  - 有稀有度：稀有度颜色 + 下划线。
  - 无稀有度：正文默认样式 + 下划线。
- 点击后弹出**通用面板弹窗**，结构统一：
  - 标题栏：名称，应用稀有度颜色或默认主题色。
  - 字段列表：`字段名：字段值`，字段名次要颜色，字段值主要颜色。
  - `提示`、`警告` 标红。
  - `？？？` 原样保留。
  - 所有原文内容不删减，只改样式。
- 面板内不区分属性/装备/技能，统一展示。

#### 3.2.3 系统提示单行框
**适用对象**：单行系统提示，如 `【提示：你已晋升至绝强者。】`、`【你的烙印等级上限提升至Lv.95。】`。

**展示方式**：
- 正文中直接包裹成单行小框。
- 每条提示独立成框，一行一框。
- `提示：` 前缀可标红或加粗。
- 内容原样保留，不弹窗。

#### 3.2.4 系统任务/奖励大框
**适用对象**：
- 任务面板：`【支线任务：宝藏。】` + 后续字段。
- 选择/奖励界面：`【真实敏捷属性...，你可在以下奖励中...】` + `A. B. C.` 选项。
- 确认/激活界面：`【是/否启动「超·界级封禁术式」。】` + 后续说明。

**展示方式**：
- 正文中整体包裹成大框。
- 内容原样保留，不弹窗。
- 标题行加粗高亮。
- 字段名次要颜色，字段值主要颜色。
- `警告`、`提示` 标红。
- 选项字母可用徽章样式，`是/否` 可做成按钮样式。
- 不套用稀有度颜色，不区分内部类型。

#### 3.2.5 数值增减高亮
**适用对象**：正文中独立的数值变化，如 `力量+5`、`HP-100`、`经验+200`。

**展示方式**：
- 正文中高亮显示，`+` 绿色，`-` 红色。
- 可配置动效（闪烁/渐变），默认开启。
- 弹窗内的数值变化同样高亮。

### 3.3 P1 功能详情
- **弹窗 UI 预设**：5 套（深色游戏风、浅色简洁风、科技蓝紫风、西幻风、玄幻风）。
- **稀有度颜色自定义**：用户可自定义稀有度名称和对应颜色，默认白绿蓝紫橙红。
- **导入导出配置**：支持将稀有度映射、UI 预设等配置导出为文件并导入。
- **规则更新入口**：插件内置规则更新入口，可推送新的解析规则。
- **数值动效开关**：可开启/关闭高亮和动效。

### 3.4 P2 功能（未来规划）
- **预设图标库**：根据装备名称匹配图标。
- **AI 图片生成**：接入 AI 绘图 API，用户自填 API Key，异步生成装备图。
- **手动标记语法**：与作者或平台合作，制定统一标记规范。
- **面板历史记录**：记录面板变化历史。

---

## 4. 信息架构与页面流程

### 4.1 信息架构
- Legado 阅读器 → 阅读页
- 阅读页内通过插件脚本实时识别并渲染增强内容。
- Legado 设置 → 插件设置页：
  - 总开关
  - 稀有度颜色映射配置
  - 弹窗 UI 预设选择
  - 数值增减高亮开关
  - 导入/导出配置
  - 规则更新入口

### 4.2 页面流程
1. 用户打开小说，阅读页加载当前页文本。
2. 插件扫描文本，识别面板类内容和系统界面类内容。
3. 根据识别结果：
   - 面板类内容：替换为可点击名称入口。
   - 系统提示：单行小框。
   - 系统任务/奖励：正文内大框。
   - 数值增减：高亮。
4. 用户点击名称入口 → 弹出通用面板。
5. 用户点击关闭 → 回到阅读页。
6. 用户可在设置中调整插件选项。

---

## 5. 解析规则

### 5.1 面板类内容识别
- **属性面板**：连续出现 `字段名：字段值`，且字段名属于常见属性字段（如名称、生命值、力量、敏捷等）。
- **装备面板**：以 `【名称（稀有度）】` 或 `【名称】` 开头，后跟 `字段名：字段值` 行。
- **技能面板**：以 `【技能名：Lv.X（类型）】` 开头，后跟多行描述。

**通用处理**：
- 提取名称和稀有度（若有）。
- 提取所有 `字段名：字段值` 对。
- 保留原文所有字符，包括 `？？？`、括号注释等。
- 面板结束条件：空行、`……`、下一个特殊块。

### 5.2 系统提示识别
- 单行 `【提示：...】` 或 `【...】`，内容较短，独立成行。
- 示例：`【提示：你已晋升至绝强者。】`

### 5.3 系统任务/奖励/确认识别
- **任务面板**：`【支线任务：...】` 或 `【主线任务：...】` 开头，后续跟 `难度等级：...` 等字段。
- **选择/奖励界面**：`【条件描述，你可在以下奖励中...】` 开头，后续跟 `A. B. C.` 选项。
- **确认/激活界面**：`【是/否启动「XXX」。】` 开头，后续跟说明。

**通用处理**：
- 整体包裹成大框，内容原样保留。
- 字段名和字段值样式区分，`警告`、`提示` 标红。

### 5.4 数值增减识别
- 格式：`属性+数字`、`属性-数字`、`属性+N`、`属性-N`。
- 支持前缀如 `获得`、`消耗`、`提升`。
- 示例：`力量+5`、`获得经验值 +200`。

---

## 6. 视觉与交互规范方向

- **名称入口**：
  - 有稀有度：稀有度颜色 + 下划线。
  - 无稀有度：正文样式 + 下划线。
- **通用弹窗**：
  - 标题栏应用稀有度颜色或默认主题色。
  - 字段名次要颜色，字段值主要颜色。
  - `提示`、`警告` 标红。
  - 内容原样保留，只改样式。
- **稀有度颜色**：默认六档：白、绿、蓝、紫、橙、红（支持自定义，以小说为单位存储）。
- **系统提示**：每句一个小框，独立成框，不合并。
- **系统任务/奖励**：正文内大框，原样保留，字段名次要色，字段值/标题主要色。
- **数值增减**：`+` 绿色，`-` 红色，可配置动效。
- **插件不修改 Legado 原生阅读主题**，仅增强识别到的特定内容。

---

## 7. 装备图片生成方案

- **MVP（P0/P1）**：文字/样式美化。名称带下划线 + 稀有度颜色，不生成实际图片。
- **P2 升级**：预设图标库匹配 → AI 生成 + 预设图标兜底，用户自填 API Key，异步生成。
- **原则**：不因图片生成影响阅读流畅性；MVP 阶段不涉及 API Key 和隐私问题。

---

## 8. 数据模型草案

### 8.1 通用面板对象（Panel）
```json
{
  "type": "panel",
  "name": "破旧的燧发枪",
  "rarity": "白色",
  "rarityColor": "#FFFFFF",
  "fields": [
    {"label": "产地", "value": "海贼王，利奥波特作坊"},
    {"label": "耐久度", "value": "6/30"},
    {"label": "攻击力", "value": "2~13（根据距离计算）"}
  ],
  "sourceRange": [0, 0]
}
```

### 8.2 系统界面对象（SystemBlock）
```json
{
  "type": "system",
  "subType": "prompt | quest | choice | confirm",
  "title": "支线任务：宝藏",
  "fields": [
    {"label": "难度等级", "value": "Lv.？？？"}
  ],
  "options": ["A. ...", "B. ..."],
  "sourceRange": [0, 0]
}
```

### 8.3 数值变化对象（ValueChange）
```json
{
  "type": "valueChange",
  "attribute": "力量",
  "change": "+5",
  "sourceRange": [0, 0]
}
```

---

## 9. 非功能需求

1. **性能**：单页解析耗时 < 50ms，不卡翻页；弹窗打开/关闭流畅。
2. **兼容性**：支持 Legado 3.x 主流版本，Android 8.0 及以上；以插件/主题/替换规则形式运行，不修改源码。
3. **稳定性**：识别错误不破坏原文，所有增强可降级为普通文本；异常时自动禁用解析。
4. **可配置性**：全局开关；稀有度颜色映射自定义；弹窗 UI 预设切换；数值动效开关；导入/导出配置。
5. **隐私与成本**：MVP 不接入 AI，所有解析在本地完成，不上传小说内容。
6. **维护性**：解析规则配置化，便于扩展；插件包结构清晰。

---

## 10. 成功指标

- 插件启用率：安装后用户主动启用比例。
- 解析准确率：面板/系统界面识别准确率 ≥ 85%（基于测试样本）。
- 弹窗打开率：用户点击名称入口的比例。
- 性能无感率：翻页帧率无明显下降。
- 用户反馈：正向反馈、功能请求、误报率。

---

## 11. 风险与开放问题

### 11.1 风险
- **自动识别误判**：不同作者写法差异大，可能误判普通文本。通过降级机制和规则配置缓解。
- **Legado 版本更新**：API 变动可能导致插件失效，需跟进更新。
- **规则更新机制**：需要设计稳定的规则推送渠道。
- **稀有度颜色自定义**：配置错误可能导致颜色混乱，需提供恢复默认选项。

### 11.2 开放问题
- 是否公开插件，是否需要在 Legado 社区推广？
- 作者手动标记语法何时引入？是否与起点等平台合作？
- 是否支持多语言或不同世界的稀有度体系？
- 插件是否免费，是否接受捐赠？

---

## 12. 未来规划

- **作者标记规范**：与头部平台合作，制定标记规范，提升识别准确率。
- **AI 图片生成**：用户自填 API Key，接入 AI 绘图，异步生成装备图。
- **预设图标库**：补充装备图标库。
- **面板历史记录**：记录角色成长轨迹。
- **多平台扩展**：考虑适配其他阅读器或自研轻量阅读器。

---
