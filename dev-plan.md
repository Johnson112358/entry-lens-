---

# 数值词条流小说阅读增强插件 开发计划（Vibe Coding 版）

> 关联文档：[PRD（prd.md）](./prd.md)
> 文档版本：v2.0
> 更新时间：2026-08-20
> 工作模式：Vibe Coding（自然语言驱动 × AI 结对 × 真机验证闭环）

---

## 1. 项目概述

### 1.1 目标
在 Legado（阅读）平台上交付"数值词条流小说阅读增强插件"：自动识别面板类内容、系统提示、任务/奖励界面与数值增减，原文不删减、只做视觉增强（可点击入口 + 通用弹窗、单行小框、大框、高亮），支持用户自定义。

### 1.2 范围
| 阶段 | 内容 | 目标版本 |
| ---- | ---- | -------- |
| MVP（第一批迭代） | PRD 3.2 全部 P0 功能 | v0.1.x |
| 增强版（第二批迭代） | PRD 3.3 全部 P1 功能 | v0.2.x |
| 未来规划 | PRD 3.4 P2 功能，进入 Backlog | — |

### 1.3 工作模式说明
本项目采用 **Vibe Coding** 方式：由"我"（开发者）用自然语言描述需求与问题，AI 生成/修改代码，我在 Legado 真机上快速验证，把结果反馈给 AI 继续修正。

**与常规开发计划的差异：**
| 维度 | 常规模式 | 本计划（Vibe Coding） |
| ---- | -------- | --------------------- |
| 计划粒度 | 任务卡 + 工时 + 甘特图 | 迭代单元 + Prompt 要点 + 验证步骤 |
| 测试 | 专职测试 + 测试计划 | 样例集跑批 + 真机冒烟清单 |
| 文档 | 详尽设计文档先行 | 轻量文档 + PROGRESS.md 持续记录 |
| 排期 | 周级里程碑 | 日级迭代循环，速度优先 |
| 质量保障 | 流程管控 | 回归样例集 + 每次改动必验 |

---

## 2. 核心工作流程（Vibe 循环）

每个功能都走同一个循环，**一次只做一个功能**：

```
┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
│ 1. 写需求 │──▶│ 2. 给样例 │──▶│ 3. AI 生成│──▶│ 4. 真机验证│──▶│ 5. 反馈修正│
│   (自然语言)│   │ (输入/输出)│   │   代码    │   │  Legado  │   │  ←──┐    │
└──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────┼────┘
                                                                   │ 通过
                                            ┌──────────────────────┘
                                            ▼
                                    6. 归档（更新 PROGRESS.md + 跑全量回归）
```

### 2.1 循环六步
1. **写需求**：用自然语言描述要做的功能，引用 PRD 对应章节（如"实现 PRD 3.2.4 的系统任务大框"）。
2. **给样例**：先准备"输入原文 → 期望渲染结果"的示例文本（真实小说语料），让 AI 有明确靶子。**样例先行**。
3. **AI 生成**：AI 生成/修改代码。约束见"附录 A：给 AI 的全局约束"。
4. **真机验证**：装进 Legado，打开测试书源，肉眼核对渲染效果；发现不对就截图/摘录原文。
5. **反馈修正**：把验证结果（截图描述、错误样例）原样反馈给 AI，回到第 3 步。一次反馈一个具体问题。
6. **归档**：功能通过后，更新 `PROGRESS.md`（完成状态、已知问题、关键决策），跑一遍回归样例集确认没破坏旧功能，Git 提交打 tag。

### 2.2 铁律
- **样例先行**：没有样例文本不动手写识别规则。
- **改动必回归**：AI 每次改完，必须跑样例集 + 真机冒烟，才能算完成。
- **原文永不丢**：任何渲染都必须基于原文包裹，验证时重点检查字符是否被删改。
- **一次一功能**：不要把多个功能塞进同一次 AI 对话。
- **失败要记录**：识别失败的样例存进 `fixtures/negative/`，作为调优素材。

---

## 3. 环境与工具准备（Day 1）

| 项 | 内容 | 说明 |
| -- | ---- | ---- |
| 真机/模拟器 | Android 8.0+，安装 Legado 3.x | 日常验证主力；另备一台低配机测性能 |
| 测试书源 | 一个本地书源，内置各类型样例文本（面板/提示/任务/奖励/数值增减） | 用 PRD §5 的示例 + 真实小说摘录拼装 |
| 代码仓库 | Git 仓库，结构：`plugin/`（主题包+JS+替换规则）、`fixtures/`（样例）、`tools/`（断言脚本）、`docs/` | 单文件职责清晰，方便 AI 定位 |
| AI 工具链 | 确定使用的 AI 编码工具 | 项目根目录放 `PRD`、`dev-plan`、`PROGRESS.md`、`AI-CONSTRAINTS.md` 作为 AI 的固定上下文 |
| 注入验证 | 快速 spike：Legado 正文 JS 注入/DOM 操作可行性 | 不通过则切换到"纯 CSS 主题包 + 替换规则包裹"方案，立即调整计划 |

**Day 1 出口标准**：插件壳子能在 Legado 里注入一段测试 JS 并生效（如给正文加一个测试样式），Git 仓库就绪。

---

## 4. 迭代计划

每个迭代 = 一个 Vibe 循环单元。按 PRD 优先级排序，全部迭代完成即发布。

### 第一批：MVP（P0）

| 迭代 | 功能 | Prompt 要点（输入给 AI） | 真机验证方式 |
| ---- | ---- | ------------------------ | ------------ |
| I0 | 注入验证 + 项目骨架 | 插件目录结构、注入入口、Legado 兼容约束 | 打开测试书源，看到注入生效（测试标记可见） |
| I1 | 面板类识别（PRD 3.2.2 / 5.1） | 三类面板触发条件 + 结束条件 + 属性词表；附 10+ 条正例/反例 | 测试书源翻页，面板区域被正确标记（临时边框可见） |
| I2 | 名称入口 + 通用弹窗（PRD 3.2.2） | 入口样式（稀有度色+下划线）、弹窗结构（标题栏/字段分级/警告标红/？？？保留）；附期望效果描述 | 点击入口弹窗，逐项核对样式与字符完整性 |
| I3 | 系统提示单行框（PRD 3.2.3） | 一行一框、前缀标红/加粗、不弹窗；附示例 | 翻页核对框体与字符 |
| I4 | 任务/奖励/确认识别与大框（PRD 3.2.4 / 5.3） | 三类子类型触发规则、大框结构、选项徽章/是否按钮、不套稀有度色 | 逐个子类型各验证 2 条样例 |
| I5 | 数值增减高亮（PRD 3.2.5 / 5.4） | 正则规则（`属性±数字`、前缀词）、+绿-红、动效（默认开） | 正文与弹窗内各验证高亮 |
| I6 | 设置页 + 总开关（PRD 4.1） | 设置项列表、开关即时生效 | 开关切换后刷新页面生效 |
| I7 | 降级与异常保护（PRD 3.2.1 / 9.3） | 不确定识别降级、异常自动禁用、渲染失败回滚 | 喂入乱排版文本，确认原文完整可读 |
| I8 | 打包 + 内测版 | 安装指引、CHANGELOG | 按指引全新安装，跑通全流程 |

### 第二批：增强版（P1）

| 迭代 | 功能 | Prompt 要点 | 真机验证方式 |
| ---- | ---- | ----------- | ------------ |
| I9 | 稀有度颜色自定义（PRD 3.3） | 映射配置 UI、默认六档、恢复默认、按小说存储 | 改色 → 刷新 → 生效；换书 → 隔离 |
| I10 | 弹窗 UI 预设 5 套（PRD 3.3） | 每套的主题变量（配色/圆角/字体），用 CSS 变量实现 | 逐套切换，弹窗与大框样式跟随 |
| I11 | 数值动效开关（PRD 3.3） | 高亮/动效独立开关 | 开关切换后即时生效 |
| I12 | 配置导入导出（PRD 3.3） | 配置序列化/反序列化、文件读写 | 导出 → 重装 → 导入 → 配置恢复 |
| I13 | 规则更新入口（PRD 3.3） | 规则版本号、更新渠道（远程配置/文件）、热更新与回滚 | 模拟下发新规则，旧规则回滚 |
| I14 | 全量回归 + 增强版发布 | 跑全部样例与冒烟清单 | 按附录 B 冒烟清单全过 |

### Backlog（P2，暂不排期）
预设图标库 → AI 图片生成（API Key + 异步 + 兜底）→ 手动标记语法 → 面板历史记录。

---

## 5. 测试与回归（Vibe 方式的质检）

### 5.1 样例集（fixtures）
- 每个识别类型准备 **≥ 10 条正例 + 若干反例**（易误判的普通文本），共 ≥ 100 条，存 `fixtures/`。
- 结构：`input.txt`（原文段落）+ `expected.json`（期望的解析结果，对应 PRD §8 数据结构）。
- **新增规则必须先补样例**，发现误报时把误报文本加入反例集。

### 5.2 断言脚本（tools/）
- 一个跑批脚本：读 `fixtures/` → 跑解析器 → 与 `expected.json` 比对 → 输出准确率。
- AI 生成，纳入每个迭代的回归步骤；**准确率 ≥ 85% 才允许进入下一个迭代**。

### 5.3 真机冒烟清单（每次迭代必做，约 5 分钟）
1. 打开测试书源，翻 5 页，确认无渲染崩溃、无页面卡顿。
2. 每种识别类型至少肉眼核对 1 条样例：样式正确、**字符与原文一致**。
3. 点击入口弹窗开/关 3 次，确认流畅、可关闭。
4. 截图记录（作为反馈 AI 的素材）。

### 5.4 性能抽查
- 每批迭代结束时测一次：长文本页解析耗时 < 50ms、翻页无感（用低配机）。
- 超标时把慢页文本作为样例反馈给 AI 优化。

---

## 6. 进度与版本管理

### 6.1 PROGRESS.md（AI 的跨会话记忆，最重要）
每次 AI 会话**开始读它、结束更新它**，记录：
- 已完成功能与验证结论
- 当前卡住的问题（附失败样例）
- 关键决策（如识别规则调整、方案变更）
- 下一步计划

### 6.2 Git 规则
- 每个迭代一个 commit，通过后打 tag（`v0.1.0-beta`、`v0.1.0`、`v0.2.0`…）。
- 任何时刻可回退到上一个 tag；AI 改崩了立即 `git revert` 而不是现场修补。

### 6.3 发布节奏
内测版（v0.1.0-beta，第一批完成后）→ 社区公测 → 增强版（v0.2.0，第二批完成后）。渠道：GitHub Release + Legado 社区。

---

## 7. 排期（日级，单人 + AI）

| 天 | 内容 | 出口标准 |
| -- | ---- | -------- |
| Day 1 | 环境准备 + I0 注入验证 + 仓库骨架 | 注入生效，仓库就绪 |
| Day 2 | I1 面板识别 + 样例集第一批 | 正例识别通过，无崩溃 |
| Day 3 | I2 名称入口 + 通用弹窗 | 弹窗样式与字符完整性核对通过 |
| Day 4 | I3 单行框 + I4 大框 | 两类系统界面渲染通过 |
| Day 5 | I5 数值高亮 + I6 设置页 | 高亮与开关生效 |
| Day 6 | I7 降级保护 + 全量回归 + 性能抽查 | 准确率 ≥ 85%，回归全过 |
| Day 7 | I8 打包 + 内测版发布 | 全新安装可跑通 |
| Day 8~10 | 内测反馈修复（每个反馈一个 Vibe 循环） | 无 P0 缺陷 |
| Day 11~13 | I9~I13 P1 功能（稀有度自定义/预设/动效/导入导出/规则更新） | P1 逐项真机通过 |
| Day 14 | I14 全量回归 + v0.2.0 发布 | 冒烟清单全过 |

> 前提说明：速度为 vibe coding 的核心收益，前提是每个循环都严格执行"样例先行 + 真机验证"；若某迭代反复卡壳（同一问题 ≥ 3 轮未解决），停下来重写该功能的 Prompt 或更换实现思路，不要硬磨。

---

## 8. 风险与应对（Vibe Coding 特有视角）

| 风险 | 等级 | 表现 | 应对 |
| ---- | ---- | ---- | ---- |
| AI 会话上下文丢失 | 高 | 新会话忘记旧决策，功能回退 | PROGRESS.md 强制读写 + 每个迭代一个 commit |
| AI 生成代码不兼容 Legado/Rhino | 高 | 注入失败或语法报错 | Day 1 注入验证先行；全局约束写入 AI-CONSTRAINTS.md（禁止浏览器专有 API、ES 版本约束） |
| 识别规则越改越乱 | 中 | 修好 A 弄坏 B | 样例集回归是硬门槛，任何改动先跑批 |
| 真机验证不充分 | 中 | 样式/字符问题漏到内测 | 冒烟清单标准化；每个迭代必做 |
| 准确率不达标（作者写法差异） | 中 | 误报/漏报集中 | 持续收集反例进 fixtures，规则调优循环；最终靠 P1 规则更新入口兜底 |
| 速度优先导致代码腐化 | 低 | 后期难以维护 | 单文件职责清晰、关键逻辑让 AI 写注释、规则配置化（数据驱动，少写死逻辑） |

---

## 9. 验收清单（对应 PRD §10 成功指标）

- [ ] 解析准确率 ≥ 85%（fixtures 跑批）
- [ ] 单页解析耗时 < 50ms，翻页无感
- [ ] 任意场景原文可完整还原（降级可靠）
- [ ] 弹窗/单行框/大框/高亮符合 PRD §6 视觉规范
- [ ] 通用弹窗：标题栏稀有度色、字段分级、`提示/警告` 标红、`？？？` 保留
- [ ] Legado 3.x × Android 8.0+ 可用
- [ ] P1 功能（预设/自定义/导入导出/规则更新/动效开关）全部可用
- [ ] PROGRESS.md 与 Git tag 完整可追溯

---

## 附录 A：给 AI 的全局约束（AI-CONSTRAINTS.md 要点）

1. 运行环境为 Legado（Rhino 系 JS 引擎 + WebView），**禁止使用浏览器专有 API**（DOM 需按 Legado 注入方式操作，ES 语法遵循项目锁定的版本）。
2. **不修改原文**：所有增强都是对原文的样式包裹，任何处理都不得增删改字符。
3. 识别不确定时**降级为普通文本**，不强行套用样式。
4. 解析必须满足单页 < 50ms：优先单次扫描 + 正则/词表匹配，禁止逐字符重型算法。
5. 解析规则**数据驱动**（正则 + 词表放在规则配置区），便于后续下发更新。
6. 改动前先看 `fixtures/`，改动后必须跑 `tools/` 断言脚本，输出全绿才算完成。
7. 样式用 CSS 变量组织，为 5 套 UI 预设留扩展位。

## 附录 B：真机冒烟清单（模板）

```
□ 打开测试书源，翻 5 页无崩溃、无卡顿
□ 面板入口：稀有度色 + 下划线；无稀有度：正文样式 + 下划线
□ 点击入口 → 弹窗：标题栏颜色、字段分级、警告标红、？？？保留
□ 弹窗开/关 3 次流畅
□ 系统提示：一行一框，字符完整
□ 任务/奖励大框：标题加粗、选项徽章、是/否按钮、不套稀有度色
□ 数值增减：+ 绿 / - 红，动效生效
□ 设置：总开关 / 动效开关 / 稀有度颜色 / UI 预设 即时生效
□ 乱排版文本：降级为普通文本，原文完整
□ 截图存档（供反馈 AI 使用）
```
