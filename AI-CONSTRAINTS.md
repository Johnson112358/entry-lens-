# AI 全局约束（AI-CONSTRAINTS.md）

> 本文件是 AI 在本仓库开发时的**强制约束**。改动代码前必读，改动后必对照。
> 关联：[dev-plan.md](./dev-plan.md) 附录 A、[PROGRESS.md](./PROGRESS.md)

## 0. 运行环境事实（2026-08 调研结论，来自 legado-E 源码）

- Legado 3.x 阅读页正文是 **Android Canvas 自绘渲染**（StaticLayout/TextPage），**不是 WebView DOM**。
- 因此**禁止假设**正文可被 JS 操作 DOM（无 document/window/querySelector）。
- 官方可用的增强机制只有两条：
  1. **替换规则（Replace Rule）**：正则匹配正文文本 → 替换为文本/HTML。`替换`字段以 `@js:` 开头时，其后的 JS 代码在 **Rhino 1.8.1**（Legado 内置）中 `eval` 执行，可用变量：`result`（本次匹配文本）、`chapter`、`book`、`java`（RegexJsExtensions 工具）。执行结果字符串作为替换内容插入。
  2. **`<usehtml>` 段落**：某个段落以 `<usehtml>` 开头、以 `<` 结尾时，该段落内容会被 `HtmlCompat.parseAsHtml` 解析并按 HTML 渲染（支持 `<font color size face>`、`<b>`、`<u>`、`<i>`、`<s>`、`<sub>`、`<sup>`、`<br>`、`<p>`、`<a href>`、`<hr>` 等子集；不支持 div/背景/边框/CSS 类）。
- 两项能力叠加 = 本项目全部渲染手段。**没有弹窗 API**（Canvas 下 `<a>` 点击只会打开"链接确认"页）。

## 1. 语法与环境约束

1. 运行环境为 **Rhino 1.8.1**（Android）：同步执行、无 DOM、无 fetch/XHR、无 Promise/async-await、无 console（可用 `java` 工具兜底但尽量不用）。
2. 核心逻辑必须 **ES5.1 兼容**（`var`、`function`、字符串拼接；禁止 let/const/箭头函数/模板字符串/解构/类/生成器）。Node 侧测试工具可用现代语法，但被 Rhino 执行的 `plugin/src/*.js` 必须 ES5。
3. 禁止浏览器专有 API；禁止 `require/module.exports` 出现在合并进替换规则的产物里（构建工具负责剥离）。

## 2. 原文永不丢（最高优先级）

1. 所有增强都是**对原文的样式包裹**：识别命中 → 在原文外增加 HTML 标签；未命中 → 原样输出。任何处理**不得增删改一个字符**（包括空格、换行）。
2. 渲染器输出 HTML 时，原文中的 `<`、`>`、`&` 必须转义（`&lt;` `&gt;` `&amp;`），标签本身用拼接生成，保证"去标签后 === 原文"。
3. `<usehtml>` 段以 `<` 结尾（Legado 取最后一段 `<` 之前的内容），因此 HTML 内禁止出现裸 `<`。
4. 回归脚本必须断言：对每个 fixture，`stripHtml(render(text)) === text`。

## 3. 识别与降级

1. 识别不确定时**降级为普通文本**，不强行套样式；宁可不增强，不可错增强（误报污染正文比漏报更坏）。
2. 所有识别逻辑必须 try/catch 包裹：**任何异常 → 返回原文**。单条规则异常不得影响整章。
3. 面板识别必须满足"字段词表/结构校验"双重条件（见 prd.md §5.1），避免把普通对话识别成面板。
4. 新增规则**必须先补 fixtures 正例+反例**（样例先行铁律），改动后必须跑 `tools/run-fixtures.js` 全绿。

## 4. 性能

1. 目标：整章解析 < 500ms（Rhino/真机），单页分摊 < 50ms；替换规则自带 3000ms 超时兜底。
2. 实现方式：**单次线性扫描 + 词表匹配**；禁止逐字符嵌套循环、禁止在循环内做正则 compile、禁止 O(n²) 字符串拼接（用数组 join）。
3. 正则只用在行级匹配；跨行结构用显式扫描（数组遍历），不写复杂回溯正则。

## 5. 数据驱动

1. 属性词表、稀有度名称/颜色、触发关键词、正则片段全部集中在 `plugin/src/el-config.js` 的配置对象里，**禁止散落写死在业务函数中**。
2. 颜色用 `#RRGGBB` 字符串；稀有度六档默认：白色 `#C0C0C0`、绿色 `#2ECC71`、蓝色 `#4A90D9`、紫色 `#9B59B6`、橙色 `#E67E22`、红色 `#E74C3C`（可调）。
3. 为 P1 的"预设切换/颜色自定义"预留配置位（配置对象结构稳定）。

## 6. 仓库与流程

1. 单文件职责清晰：`el-config.js`（配置）→ `el-core.js`（解析：文本→块对象）→ `el-render.js`（渲染：块/文本→HTML）。**解析与渲染分离**，fixtures 断言的是解析结果（expected.json 对应 prd.md §8 数据模型）。
2. 每个迭代一个 commit，通过后打 tag；改动崩了先 `git revert`，不现场乱修。
3. `PROGRESS.md` 每次会话结束必须更新（完成项/卡点/决策）。
4. 所有文档与代码注释用中文（面向用户文档），代码内注释中文。

## 7. 已知平台限制（勿试图绕过）

| 限制 | 表现 | MVP 对策 |
| ---- | ---- | -------- |
| 无正文 DOM/JS 注入 | 无法操作阅读页 DOM | 替换规则 + `<usehtml>` 方案（本仓库已定型） |
| 无弹窗 API | 点击无法弹出自定义面板 | 面板改为"内嵌美化块"渲染；`<a>` 入口仅做样式（点击行为交回 Legado） |
| Canvas 无 CSS/动效 | 无 CSS 类/动画 | 颜色/字号/粗细/下划线/横线（`<hr>`）实现视觉层级；动效标记 P1 |
| 替换规则作用于整章 | 非逐页 | 单条主规则整章处理一次，性能按整章预算 |
| `@js:` 每匹配 eval 一次 | 匹配次数多则慢 | 主规则 pattern 只匹配一次全文（`(?s)^[\s\S]*$`），其余规则做辅助开关 |
| 段落 HTML 需"适配特殊样式" | 用户需开启设置 | 安装文档明确标注前置条件（docs/INSTALL.md） |
