# CHANGELOG

## v0.1.0-beta（2026-08-21）— MVP 第一批（P0 全部）

### 功能
- 面板类识别与渲染（装备/属性/技能面板）：
  - 名称稀有度着色（六档：白绿蓝紫橙红，含别名映射）+ 下划线；无稀有度 = 正文样式 + 下划线
  - 字段名次要色、字段值正文色分级；`提示`/`警告` 标红；`？？？` 原样保留
  - 上下分隔线；技能面板支持"技能名：Lv.X（类型）"形态
- 系统提示单行框：一行一框、`提示：`/`警告：` 前缀标红加粗、不合并
- 任务/奖励/确认大框：
  - quest（支线/主线/每日/隐藏任务…）、choice（奖励选择，A. B. C. 徽章）、confirm（是/否 按钮样式）
  - 标题金色加粗，字段分级，不套稀有度色
- 数值增减高亮：`+` 绿 / `-` 红（词表校验防误报；支持前缀词/英文缩写/组合形态）
- 降级与异常保护：识别不确定 → 原样文本；任何异常 → 返回原文；乱排版/对话不误伤
- 原文完整性：全量样例断言"去标签后 === 原文" 100% 通过

### 质量
- fixtures 样例集：**100 条**（正例 61 / 反例 35 / 混合章节 4），解析准确率 **100%**
- 性能：5000 字整章渲染 **0.55ms**（Node 基准；目标 <50ms）
- 产物冒烟：replace-rules.json 在 vm 模拟 Rhino eval 下整章处理、原文保真、普通文本原样 全部通过

### 已知限制（平台能力决定，详见 docs/INSTALL.md §4）
- 无点击弹窗（Legado 自绘渲染无弹窗 API）→ 面板以完整美化块展示
- 无数值动效（无 CSS/动画能力）→ 仅颜色高亮
- 无独立设置页 → 开关 = 启用/停用规则；颜色自定义 = 编辑规则文本

### 安装
见 `docs/INSTALL.md`（导入 replace-rules.json + 开启"适配特殊样式"）。

## Backlog（P1/P2）
- P1：稀有度颜色自定义 UI、弹窗 UI 预设、数值动效开关、配置导入导出、规则更新入口
- P2：图标库、AI 图片生成、手动标记语法、面板历史
